
<div class="container-fluid pt-5 private">
   <div class="row mt-5">
      <div class="media col-10 mx-auto d-flex justify-content-center">
         <img src="{{user.picture}}" class="img-profile img-profile align-self-center mr-3 col-lg-3 col-12" alt="profile image">
         <div class="media-body col-lg-7 col-sm-12 d-flex flex-column ml-5" style="height: 100%;">
            <h1 class="mt-0 mb-4">{{user.name}}</h1>
            <h4 class="grey mb-4">{{user.email}}</h4>
            <a class="btn mb-4" href="recipe/{{user._id}}/recipe-book">My Recipe Book</a>
            <a class="justify-self-end mt-auto" href="/{{user._id}}/edit" style="width: 100%;">Edit profile</a>
         </div>
      </div>
   </div>
</div>